c++:

class Solution {
public:
    vector<int> limitOccurrences(vector<int>& nums, int k) {
        int i=0;
        for(int n:nums){
            if(i<k ||n!= nums[i-k]){
                nums[i++]=n; 
            }
        }
        nums.resize(i);
        return nums;
    }
};
