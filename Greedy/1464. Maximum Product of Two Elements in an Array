class Solution {
public:
    int maxProduct(vector<int>& nums) {
        //Approach 1 : Time complexity :O(n^2) and space complexity : O(1).
        // int n=nums.size();
        // int maxp=INT_MIN;
        // for(int i=0;i<n;i++){
        //     for(int j=i+1;j<n;j++){
        //        if(maxp<(nums[i]-1)*(nums[j]-1)){
        //         maxp=max(0,(nums[i]-1)*(nums[j]-1));
        //        }
        //     }
        // }
        // return maxp;

        //approach 2:  Time complexity :O(nlogn + logn) and space complexity : O(1).
        //  int n=nums.size();
        //  sort(nums.begin(),nums.end());
        //  return (nums[n-1]-1)*(nums[n-2]-1);


        //Approach 3: Time complexity :O(n) and space complexity: O(1).
        int max1=INT_MIN;
        int max2=INT_MIN;
        for(int x:nums){
            if(x>max1
            ){
                max2=max1;
                max1=x;
            }
            else if(x>max2){
                max2=x;
            }
        }
        return (max1-1)*(max2-1);
    }
};
