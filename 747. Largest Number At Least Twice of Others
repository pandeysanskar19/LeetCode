#c++

class Solution {
public:
    int dominantIndex(vector<int>& nums) {
        auto it = max_element(nums.begin(), nums.end());
        int mxidx = it-nums.begin();
        for(int i : nums)
            if(i > (*it)/2 && i != *it)
                return -1;
        return mxidx;
    }
};
