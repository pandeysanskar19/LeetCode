class Solution {
public:

    int longestSubsequence(vector<int>& nums) {
        int n=nums.size();
        int xr=0;
// METHOD 1:  BRUTE FORCE    TIME COMPLEXITY : O(N^2);  SPACE COMPLEXITY : O(1).
        /*int ans=0;
        int cnt =0;
       for(int i=0;i<n;i++){
         xr=0;
         cnt=0;
         for(int j=i;j<n;j++){  
             xr ^= nums[j];
             cnt++;
         }
         if(xr !=0) ans=max(cnt,ans);
        
        }
     return ans;*/
//Method 2: optimal      TIME COMPLEXITY: O(N) ; SPACE COMPLEXITY: O(1)
        int nonzero=0;
        for(int right=0;right<n;right++){
            xr ^= nums[right];
            
            if(nums[right] !=0) nonzero =1;
            
        }
        if(xr !=0) return n;

        if(nonzero) return n-1;
        return 0;
    }
};
